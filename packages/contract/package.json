{
  "name": "@yoyaflow/yoya-ui-contract",
  "version": "0.7.6",
  "private": true,
  "description": "Cross-package contract tests: core ⇄ yoya-ui boundaries (identity, singleton, keyed protocol, SSR byte-equality, compile channels) plus the whole-repo gates that neither line owns alone.",
  "type": "module",
  "scripts": {
    "test": "vitest run --project contract",
    "test:dist": "vitest run --project contract --dir tests/dist"
  },
  "license": "MIT"
}
