# 性能基准（官方 js-framework-benchmark）

这一页放的是 yoya-ui 在官方 [js-framework-benchmark](https://github.com/krausest/js-framework-benchmark)
（`keyed` 分类）上的实测数字：**AST 编译版（`yoya-ui-ast`）**对比官方原生基线 `vanillajs`，
同时并列对照条目（同轮测量：运行期版 `yoya-ui-runtime`、Vue、React、Solid、Svelte），
便于看改动带来的位移与横向位置。报告页里 yoya 更快/更慢的格子有底色区分（±5% 内视作持平）。

数字不是手写的：表格块由 `benchmark/results.json` 生成，
`npm run report:bench:write` 刷新、`npm run verify:dist` 校验，手工改数字会直接失败。
复跑步骤见 [benchmark/README.md](../benchmark/README.md)。

<!-- benchmark:tables:start 由 scripts/benchmark-report.mjs 生成 -->

**运行口径**：官方 runner `playwright`（headless）+ Chrome for Testing 152.0.7977.64；CPU 项取 15 个样本的中位数（单轮 15 次迭代；多轮合并后取中位），内存 / 体积 / 首屏各 1 次采样；测量日期 2026-09-25，yoya 版本 `0.7.3`（`e21f35d`）。

**九项标准操作（ms，中位数）**

| 操作              | yoya 本次 | 原生 vanillajs | 本次 ÷ 原生 |
| ----------------- | --------- | -------------- | ----------- |
| 01 创建 1000 行   | 35.3      | 30.5           | 1.16×       |
| 02 替换 1000 行   | 38.5      | 32.8           | 1.17×       |
| 03 每 10 行改文案 | 20.5      | 22.4           | 0.92×       |
| 04 选中一行       | 5.8       | 7.3            | 0.79×       |
| 05 交换两行       | 26.9      | 22.1           | 1.22×       |
| 06 删除一行       | 18.0      | 16.8           | 1.07×       |
| 07 创建 10000 行  | 395.7     | 330.4          | 1.20×       |
| 08 追加 1000 行   | 39.7      | 34.4           | 1.15×       |
| 09 清空 ×8        | 20.6      | 15.3           | 1.35×       |

**script / paint 分解（ms，中位数）**

| 操作              | yoya script | yoya paint | 原生 script | 原生 paint |
| ----------------- | ----------- | ---------- | ----------- | ---------- |
| 01 创建 1000 行   | 5.5         | 29.2       | 2.2         | 27.8       |
| 02 替换 1000 行   | 8.6         | 29.1       | 4.3         | 27.6       |
| 03 每 10 行改文案 | 1.6         | 17.5       | 0.8         | 19.1       |
| 04 选中一行       | 0.8         | 4.1        | 0.4         | 5.6        |
| 05 交换两行       | 3.0         | 20.9       | 0.4         | 19.9       |
| 06 删除一行       | 1.4         | 15.3       | 0.4         | 15.3       |
| 07 创建 10000 行  | 62.4        | 329.4      | 23.7        | 301.1      |
| 08 追加 1000 行   | 6.2         | 32.3       | 2.2         | 31.5       |
| 09 清空 ×8        | 17.1        | 2.0        | 11.6        | 1.8        |

**内存 / 体积 / 首屏**

| 指标                      | yoya 本次 | 原生 vanillajs | 本次 ÷ 原生 |
| ------------------------- | --------- | -------------- | ----------- |
| 21 就绪内存（MB）         | 1.33      | 1.02           | 1.31×       |
| 22 建 1000 行后内存（MB） | 4.12      | 2.44           | 1.69×       |
| 25 建+清空后内存（MB）    | 1.64      | 1.16           | 1.41×       |
| 41 体积未压缩（KB）       | 76.6      | 11.7           | 6.55×       |
| 42 体积 brotli（KB）      | 20.6      | 2.5            | 8.24×       |
| 43 首屏绘制（ms）         | 382.5     | 265.4          | 1.44×       |

<!-- benchmark:tables:end -->

## 怎么读

- **script / paint 拆解**：官方 runner 把每次操作拆成 JS 执行（script）与渲染（layout / paint / commit）。
  yoya-ui 的绘制桶除 `07` 外都与原生基线持平（`01`～`06` / `08` / `09` 差 −1.6～+1.5 ms，落在噪声内），
  剩余差距是 script 桶、`07` 那一格的绘制差与内存。
- **create 类操作是主战场**：`01 / 07 / 08` 三项的 script 差距最大（1k 行 +3.3 ms、10k 行 +38.7 ms），
  对应节点对象构造、登记与首次写 DOM 的成本；`03 / 04 / 05 / 06` 这类增量操作已经追平甚至反超原生
  （`03` 20.5 ms vs 原生 22.4 ms，`04` 5.8 ms vs 原生 7.3 ms）。
- **清空路径是剩下的稳定差距**：`09_clear1k_x8` 20.6 ms，原生 15.3 ms（script 17.1 ms vs 11.6 ms），
  这是销毁路径改单趟 + 批量摘除之后的残余成本（改之前是 58 ms）。
- **`04` / `03` 反映的还有条目自身的状态模型**：yoya 条目用共享 `selectedId` 派生每行的选中类
  （`computed(() => selectedId.value === row.id)`），一次点击会唤醒全部行——与 Vue 条目的
  `:class` + `v-memo` 同形；React 条目把选中态按行传入并用 `memo` 比较；只有手写的原生基线只碰
  受影响的两行。看 `04` 这一格时要记得这一点；O(1) 的替代建模见 `docs/component-authoring.zh-CN.md` §6.2。
- **内存仍是最大量级差**：建 1000 行后 4.12 MB vs 原生 2.44 MB（1.69×）；剩余部分主要是 DOM 对象与
  V8 节点对象头。
- **体积是已知代价**：单页体积 76.6 KB / 20.6 KB brotli vs 原生 11.7 KB / 2.5 KB brotli，这是
  「一套 DSL + 组件库」与「手写 DOM 调用」的固有差别（编译版条目把片段与运行期钩子一起打进产物）。
  0.7.1 起 `@yoyaflow/yoya-ui/core` 本身就是**转发入口**（转发到 `@yoyaflow/yoya-core`），业务代码与
  编译产物天然共用同一份运行期实例——编译版只比运行期版大 ~4%（76.6 KB vs 73.9 KB）。历史上两者是
  两个不同模块图（自包含 vs 模块化），一个 bundle 里会带**两份运行期**（145.6 KB，+76%），编译出来的
  行还会订阅在第二个信号实例上；现在这条坑由包自身的入口面封住，条目侧不再需要 alias。
- **first paint 是单次采样**：官方 runner 只采一次，本机多次重复的实测区间约 355～920 ms
  （本轮 382.5 ms），表里那一格不要当精确值看；它也因此不适合用来判断 KB 级体积变化。

## 跨口径不可比

开发期还用过一套自建 harness（msedge headless，按 1000 行的 MB 增量取数）做改动前后的同机对照，
脚本属于一次性的工作现场（`.scratch/`，已随 2026-09-25 的清理删除；需要复跑就照这里的口径重写）。
**两套口径的绝对值不可互换**：官方 runner 用的是 Chrome for Testing 152 + playwright、按操作计时；
自建口径直接读 `performance.measureUserAgentSpecificMemory()` 的行增量。
同一份代码在两套口径下的读数差可达 1.5～3 倍，所以对比只能在口径内纵向进行。
