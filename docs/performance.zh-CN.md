# 性能基准（官方 js-framework-benchmark）

这一页放的是 yoya-ui 在官方 [js-framework-benchmark](https://github.com/krausest/js-framework-benchmark)
（`keyed` 分类）上的实测数字：**AST 编译版（`yoya-ui-ast`）**对比官方原生基线 `vanillajs`，
同时并列对照条目（同轮测量：运行期版 `yoya-ui-runtime`、Vue、React、Solid、Svelte），
便于看改动带来的位移与横向位置。报告页里 yoya 更快/更慢的格子有底色区分（±5% 内视作持平）。

数字不是手写的：表格块由 `benchmark/results.json` 生成，
`npm run report:bench:write` 刷新、`npm run verify:dist` 校验，手工改数字会直接失败。
复跑步骤见 [benchmark/README.md](../benchmark/README.md)。

<!-- benchmark:tables:start 由 scripts/benchmark-report.mjs 生成 -->

**运行口径**：官方 runner `playwright`（headless）+ Chrome for Testing 152.0.7977.64；CPU 项取 15 个样本的中位数（单轮 15 次迭代；多轮合并后取中位），内存 / 体积 / 首屏各 1 次采样；测量日期 2026-09-20，yoya 版本 `0.6.11`（`cc61971`）。

**九项标准操作（ms，中位数）**

| 操作              | yoya 本次 | 原生 vanillajs | 本次 ÷ 原生 |
| ----------------- | --------- | -------------- | ----------- |
| 01 创建 1000 行   | 34.5      | 30.7           | 1.12×       |
| 02 替换 1000 行   | 37.7      | 32.1           | 1.17×       |
| 03 每 10 行改文案 | 23.6      | 21.5           | 1.10×       |
| 04 选中一行       | 5.9       | 7.4            | 0.80×       |
| 05 交换两行       | 148.5     | 22.9           | 6.48×       |
| 06 删除一行       | 17.7      | 17.1           | 1.04×       |
| 07 创建 10000 行  | 407.4     | 365.3          | 1.12×       |
| 08 追加 1000 行   | 38.9      | 35.1           | 1.11×       |
| 09 清空 ×8        | 23.7      | 16.1           | 1.47×       |

**script / paint 分解（ms，中位数）**

| 操作              | yoya script | yoya paint | 原生 script | 原生 paint |
| ----------------- | ----------- | ---------- | ----------- | ---------- |
| 01 创建 1000 行   | 5.2         | 28.5       | 2.1         | 28.1       |
| 02 替换 1000 行   | 8.3         | 28.7       | 4.3         | 27.2       |
| 03 每 10 行改文案 | 1.6         | 19.6       | 0.8         | 17.5       |
| 04 选中一行       | 0.8         | 4.2        | 0.4         | 5.9        |
| 05 交换两行       | 19.7        | 125.6      | 0.4         | 20.6       |
| 06 删除一行       | 0.8         | 15.8       | 0.4         | 16.0       |
| 07 创建 10000 行  | 55.9        | 339.4      | 24.9        | 332.2      |
| 08 追加 1000 行   | 5.2         | 32.7       | 2.2         | 32.1       |
| 09 清空 ×8        | 19.9        | 2.2        | 11.9        | 2.0        |

**内存 / 体积 / 首屏**

| 指标                      | yoya 本次 | 原生 vanillajs | 本次 ÷ 原生 |
| ------------------------- | --------- | -------------- | ----------- |
| 21 就绪内存（MB）         | 1.34      | 1.05           | 1.28×       |
| 22 建 1000 行后内存（MB） | 3.99      | 2.44           | 1.64×       |
| 25 建+清空后内存（MB）    | 1.69      | 1.09           | 1.56×       |
| 41 体积未压缩（KB）       | 78.2      | 11.7           | 6.68×       |
| 42 体积 brotli（KB）      | 21.1      | 2.5            | 8.44×       |
| 43 首屏绘制（ms）         | 381.9     | 269.2          | 1.42×       |

<!-- benchmark:tables:end -->

## 怎么读

- **script / paint 拆解**：官方 runner 把每次操作拆成 JS 执行（script）与渲染（layout / paint / commit）。
  yoya-ui 的绘制桶已经与原生基线持平（9 项里差 0.2～3 ms，落在噪声内），剩余差距全部在 script 桶与内存。
- **create 类操作是主战场**：`01 / 07 / 08` 三项的 script 差距最大（1k 行 ~18 ms、10k 行 ~195 ms），
  对应节点对象构造、登记与首次写 DOM 的成本；`03 / 04 / 05 / 06` 这类增量操作已经接近原生。
- **清空路径已经追平**：`09_clear1k_x8` 从 58 ms 降到 22.5 ms（原生 15.5 ms），这是销毁路径改单趟 +
  批量摘除的结果。
- **`04` / `03` 反映的还有条目自身的状态模型**：yoya 条目用共享 `selectedId` 派生每行的选中类
  （`computed(() => selectedId.value === row.id)`），一次点击会唤醒全部行——与 Vue 条目的
  `:class` + `v-memo` 同形；React 条目把选中态按行传入并用 `memo` 比较；只有手写的原生基线只碰
  受影响的两行。看 `04` 这一格时要记得这一点；O(1) 的替代建模见 `docs/component-authoring.zh-CN.md` §6.2。
- **内存仍是最大量级差**：建 1000 行后 3.92 MB vs 原生 2.43 MB（1.61×）；剩余部分主要是 DOM 对象与
  V8 节点对象头。
- **体积是已知代价**：单页体积 83.8 KB / 20.9 KB brotli vs 原生 11.7 KB / 2.5 KB brotli，这是
  「一套 DSL + 组件库」与「手写 DOM 调用」的固有差别（编译版条目把片段与运行期钩子一起打进产物）。
- **first paint 是单次采样**：官方 runner 只采一次，本机多次重复的实测区间约 300～430 ms（本轮
  339.5 ms），表里那一格不要当精确值看；它也因此不适合用来判断 KB 级体积变化。

## 跨口径不可比

本仓库还维护一套自建 harness（`.scratch/perf-1x/`，msedge headless，按 1000 行的 MB 增量取数），
用于改动前后做同机对照。**两套口径的绝对值不可互换**：官方 runner 用的是 Chrome for Testing 152 +
playwright、按操作计时；自建 harness 直接读 `performance.measureUserAgentSpecificMemory()` 的行增量。
同一份代码在两套口径下的读数差可达 1.5～3 倍，所以对比只能在口径内纵向进行。
