# 核心库基准报告

口径、可比性规则与完整测量方法见 `benchmarks/PLAN.md`。

## 环境

- browser: msedge / 136.0.3240.64
- machine: win32 10.0.26200 x64 · CPU × 28
- node: v22.21.1 · package: 0.6.0 · commit: da226fe
- 每操作测量轮数：15
- date: 2026-09-17T03:54:47.352Z

## 标准操作（keyed，核心库基础元素）

| 操作       | 中位数   | p95      | 工作量   | 行数  | DOM 节点 | 护栏 |
| ---------- | -------- | -------- | -------- | ----- | -------- | ---- |
| `run`      | 42.3 ms  | 58.1 ms  | 18.3 ms  | 1000  | 7002     | 通过 |
| `replace`  | 55.3 ms  | 88.9 ms  | 30.5 ms  | 1000  | 7002     | 通过 |
| `runlots`  | 509.7 ms | 602.6 ms | 244.9 ms | 10000 | 70002    | 通过 |
| `add`      | 50.4 ms  | 55.8 ms  | 20.8 ms  | 2000  | 14002    | 通过 |
| `update`   | 17.8 ms  | 33.7 ms  | 0.4 ms   | 1000  | 7002     | 通过 |
| `select`   | 5.9 ms   | 13.8 ms  | 0 ms     | 1000  | 7002     | 通过 |
| `remove`   | 12.6 ms  | 19.3 ms  | 0.6 ms   | 999   | 6995     | 通过 |
| `swaprows` | 29.4 ms  | 36.2 ms  | 4 ms     | 1000  | 7002     | 通过 |
| `clear`    | 16.7 ms  | 28.9 ms  | 12 ms    | 0     | 2        | 通过 |

### 指标说明

- **中位数 / p95 / IQR**：点击 → DOM 稳定（用户可感知成本；headless Chromium 下下限约一帧）。
- **工作量**：点击 → 最后一次 DOM 变动（引擎实际工作量，不受帧率影响）。
- **行数 / DOM 节点**：操作结束后应有的行数与真实元素数。
- **护栏**：确定性回归校验（见下），护栏失败即整个运行失败。

## 启动（多张新页面取中位数）

- 客户端 `bindTo()`：ready 33 ms · 首次绘制 28 ms
- SSR + hydrate：服务端渲染 0.9 ms · 客户端接管 0.6 ms · ready 32.6 ms

## 内存

- 强制 GC 后采样堆占用（MB）。
- 初始 2.57 · 1k 行 16.75 · 10k 行 141.21 · clear 后 11.72
- clear 后未回落 9.15 · 10 轮建/清后相对初始 16.54

## 可比性与口径

- 同一台机器、同一浏览器版本内的版本间比较才有意义；上方的环境指纹是结果的一部分。
- 中位数含约一帧的绘制延迟（headless Chromium），「工作量」一列已把它排除。
- 体积数字取自产物报表（已由 README 体积表门禁校验），基准不重复测量。

## 确定性护栏

护栏是硬失败：字段更新不得发生整行重建（行构建 0 次、子节点变动 0 次）、重排与追加后既有行节点必须保留、每个操作的行数 / id / 标签都要与期望一致。

## 体积（引自产物报表）

- `yoya.core.js`: 21.6 KB min+gzip
- `yoya.ui.js`: 94.2 KB min+gzip
- `yoya.ui-router.full.js`: 106.5 KB min+gzip
