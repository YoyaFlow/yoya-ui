# yoya-ui 路线图

> 当前版本：0.5.2。本文档只描述方向与阶段目标，任务规格与实现细节不在此展开。

## 一句话方向

**现在**：夯实标准、夯实组件库、消除 bug，把 0.5 这条线做扎实，走向 1.0 契约冻结。
**未来**：适配 GenUI，成为 Web 端「千人千面」的首选基础库——同一个页面按用户与场景
长成不同形态，而生成出来的界面直接落在真实 DOM 上。

## 已完成（0.2 → 0.5，简述）

- **产物形态**：增量入口（core / ui / router / echart / three / devtools，共享 core chunk）、
  自包含 full 与 UMD；`exports` 收敛，SSR 原语并入 `./router`。
- **状态模型**：统一到 Signals（`ref` / `computed`），移除节点级 `state` / `setState`
  与 `vStateNode`；引擎可插拔，插件由使用者自己写（core 只给契约、模板与一致性用例）。
- **能力面**：组件分类子入口、SSR / hydrate、按请求 i18n、主题 token、权限声明、
  DevTools（Beta）、可选扩展入口。
- **质量基线**：140 个测试文件、1000+ 用例；消费方类型测试；`verify:dist` 门禁
  （分类隔离、SSR 单 core 冒烟、体积预算）。

## 现在：夯实标准、夯实组件库、消除 bug（0.5.x → 1.0）

### 夯实标准

- **契约三处对齐**：组件形态、值位置、事件与生命周期在 skill、`docs/`、类型声明里
  必须说同一件事；发现偏差就改，而不是各留一份。
- **类型即契约**：类型不得比运行时窄或宽（例如 `text()` 必须同时接受文本与句柄）；
  每个入口继续由消费方类型测试覆盖。
- **示例即可复制**：示例站的代码可运行、可复制，且与文档表述一致；演示不允许用
  违反规范或默认不安全（如 `innerHTML` 拼数据）的写法。
- **默认即正确**：默认转义、SSR 输出确定、无隐藏全局状态；安全性不由使用方
  额外补丁承担。

### 夯实组件库

- **按场景补缺口**：以真实业务场景（后台、C 端页面、控制台/大屏、三方互操作）为
  准补组件，不按别的 UI 库的清单堆数量。
- **三端一致**：同一组件在 SPA、SSR/hydrate、脚手架模板里行为一致；键盘可达、
  无障碍属性、加载/空/错误态在每个组件里都有交代。
- **开箱质量**：主题 token 消费、明暗与密度适配、移动端触摸与窄屏布局。

### 消除 bug

- **缺陷优先于新功能**：修完即补回归用例（本次 `vTree` 图标与演示 `innerHTML`
  就是按这条处理的）。
- **门禁不放水**：`lint` / `format:check` / `typecheck` / `test` / `build` /
  `verify:dist` 全绿才算完成，不用「先合并后修」。
- **已知债只减不增**：冻结的演示行数预算、示例站里仍从增量入口引 core 符号的
  单文件演示等，逐条清掉并登记。

## 1.0：把契约冻结下来

- 组件形态、生命周期、入口边界作为**稳定契约**公布；文档、skill、类型与实现四者
  一致后宣布冻结。
- **版本号从 1.0 起变为 `1.<年份>.<修复序号>`**（如 `1.2026.0`、`1.2026.1`、
  `1.2027.0`）：年份段是发布年份，末位是当年内 bug 修复版本的递增；不再有 2.0。
- **核心库基本 API 不再更新**：1.0 之后只做 bug 修复、文档、内部实现，以及不改变
  既有契约的补充；需要迁移指南的场景默认不存在（口径见 README「版本策略」）。

## 未来：面向 GenUI 的 Web 基础库

目标不是「再做一个组件库」，而是让生成的界面能直接落到真实 DOM，并让同一份页面
按用户与场景长成不同形态（千人千面）。

- **生成友好**：契约明确、示例可复制、无框架上下文与构建魔法——AI 一次生成即可
  运行；skill + 类型 + 示例共同构成生成指引。
- **语义化组合**：结构由数据与组件组合决定，外观由主题 token 决定；换皮、换布局
  不需要重写组件。
- **运行期个性化**：主题、布局壳、权限、i18n 都是运行期输入，同一页面按用户、
  租户、场景切换形态，而不必维护多套页面。
- **可验证**：生成结果用一致性用例与 SSR 快照校验，而不是靠人眼；生成质量的回归
  能被测试拦住。
- **边界**：不引入框架锁定、不内置厂商 SDK；扩展继续走「使用者自己写插件」的路。
