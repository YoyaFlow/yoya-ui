# 编译路径对比报告（形状矩阵基准）

> 由 `npm run report:shapes` 生成，数据源 `benchmark/shapes.json`。同一份行源码两列对照：
> **通用**（不挂编译器）与 **编译**（真编译产物）；列表对账两条路共用，差额 = 每行建造成本。

## 环境

- 日期：2026-09-23
- 机器：win32 10.0.26200 · Intel(R) Core(TM) i7-14700HX
- 浏览器：Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) HeadlessChrome/153.0.0.0 Safari/537.36 Edg/153.0.0.0
- 夹具：`benchmark/shapes/row.fixture.js`（片段 194 B · 活结点 2）
- DOM 逐字节一致（两列）：是

## 一、构建类操作（越省越好）

| 操作 | 通用 | 编译 | 编译 ÷ 通用 | 省下 |
| --- | --- | --- | --- | --- |
| create 1k | 11.10 ms | 5.80 ms | **0.52×** | 48% |
| replace 1k | 13.10 ms | 6.50 ms | **0.50×** | 50% |
| append 1k | 10.90 ms | 6.50 ms | **0.60×** | 40% |
| create 10k | 139.10 ms | 75.40 ms | **0.54×** | 46% |

## 二、对账类操作（不该退化）

| 操作 | 通用 | 编译 | 编译 ÷ 通用 |
| --- | --- | --- | --- |
| update every 10th ×16 | 0.525 ms | 0.475 ms | 0.90× |
| select one（交替写） | 0.001 ms | 0.001 ms | 0.50× |
| swap two | 0.284 ms | 0.246 ms | 0.87× |
| remove one | 0.085 ms | 0.090 ms | 1.06× |
| clear 1k | 1.500 ms | 0.800 ms | 0.53× |

## 三、行规模矩阵（create 1k，列 = 每行元素数）

| 每行元素数 | 片段 | 通用 | 置换 | 置换 ÷ 通用 | DOM 一致 |
| --- | --- | --- | --- | --- | --- |
| 4 | 163 B | 9.50 ms | 4.30 ms | **0.45×** | 是 |
| 8 | 287 B | 15.40 ms | 5.20 ms | **0.34×** | 是 |
| 16 | 547 B | 29.40 ms | 7.00 ms | **0.24×** | 是 |
| 32 | 1075 B | 54.90 ms | 9.50 ms | **0.17×** | 是 |

另有 `{ ...rest, vn }` 形状（票 18）：通用 12.30 ms vs 片段 + 运行期合并 5.40 ms（**0.44×**）。

## 结论

- **收益为正的形状集合**：每行 4 列（0.45×）、每行 8 列（0.34×）、每行 16 列（0.24×）、每行 32 列（0.17×）
- 构建类省 45–50%，对账类（select / swap / remove / update / clear）不退化；
- **行越大越省**（通用随元素数线性涨、置换几乎是平的）→ 真机上没有"太小所以别编"的规模线，
  小形状只是绝对收益小；
- 深度取舍只看真机：同一批形状在 jsdom 里置换**更慢**（1.2–1.6×），jsdom 只留排序结论。
