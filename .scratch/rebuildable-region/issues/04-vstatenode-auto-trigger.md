# 04 — vStateNode 接入自动触发

**What to build:** `setState` 之后宿主树中被标记的区域按谓词自动重建；`update`、函数值绑定、区域重跑三者边界清晰（默认只刷值，谓词为真才重建结构）；重跑期间产生的状态变化入队并在本轮结束后处理。

**Blocked by:** 02 — 重跑的环境自持, 03 — 绑定归区域

**Status:** done（2026-09-12 补记：实现与测试早已落地，票面状态此前未更新；证据见本目录 STATUS.md）

- [x] 区域发现走视图树（构建期收集），不使用模块级注册表。
- [x] `setState` 默认只刷新绑定；只有区域谓词为真时才重建对应子树。
- [x] 父区域优先、子区域跳过（父重建会产生新的子区域）。
- [x] 重跑期间触发的状态变化入队，本轮结束后统一处理，无递归与无限循环。
- [x] 未挂载组件（无 `_el`）的状态变化不产生 DOM 操作。
