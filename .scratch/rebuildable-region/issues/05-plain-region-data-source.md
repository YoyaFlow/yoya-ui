# 05 — 普通区域的数据源声明与带参值函数

**What to build:** 不在 `vStateNode` 内的区域也能使用带参值函数 `(state) => value`：显式声明数据源后，参数即数据源返回值。

**Blocked by:** 03 — 绑定归区域

**Status:** done（2026-09-12 补记：实现与测试早已落地，票面状态此前未更新；证据见本目录 STATUS.md）

- [x] 区域声明数据源后，带参值函数每次求值拿到数据源返回值。
- [x] 未声明数据源却使用带参值函数时直接报错，不静默传 `undefined`。
- [x] 组件树内的区域默认继承宿主状态，无需额外声明。
